apply_config: apply config files (make symlinks)
add_repos: add chaoticaur and arch4edu repositories
dependencies: install all dependencies
howdy_setup: install howdy and setup pam configs for it
antivirus: install and setup clamav
apps: add symlinks for applications
aesthetics: >-
  1. Copy custom icons
setup_docker: >-
  Setup user to be able to use docker:
  1. Create docker user group (catch error if group exists and continue)
  2. Add user to docker group and apply group to current session with `newgrp docker`
  3. enable and start service
copy_global_configs: >-
  1. 1password
  2. greetd
  3. *howdy - pass
  4. keyd
setup_silicon: symlink config and build cache
