To remove borders and round corners add this `userChrome.css` to `~/.zen/{your_profile_folder}/chrome` folder

# > [!NOTE]
> To remove borders you can avoid hustle with userChrome.css and
> go to `about:config` and set `zen.theme.content-element-separation` = `0`
